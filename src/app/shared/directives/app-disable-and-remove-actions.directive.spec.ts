import { AppDisableAndRemoveActionsDirective } from './app-hide-on-routes-match.directive';

describe('AppDisableAndRemoveActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDisableAndRemoveActionsDirective();
    expect(directive).toBeTruthy();
  });
});
